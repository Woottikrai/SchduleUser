import React from "react";
type RowTable = {
  className?: string;
  fullname: string;
  email: string;
  position: string;
  image?: string;
  tel?: string;
};
export default function RowTable({
  className,
  fullname,
  email,
  image,
  tel,
  position,
}: RowTable) {
  return <></>;
}
